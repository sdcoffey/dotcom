import React from "react";
import styled from "styled-components";

export interface {{ propName name }} {}

const {{ pascalCase name }}: React.FC<{{ propName name }}> = (props: {{ propName name }}) => {
  return <Container className="{{ pascalCase name }}"></Container>;
};

const Container = styled.div``;

export default {{ pascalCase name }};
